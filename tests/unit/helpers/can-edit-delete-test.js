import { canEditDelete } from '../../../helpers/can-edit-delete';
import { module, test } from 'qunit';

module('Unit | Helper | can edit delete');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = canEditDelete(42);
  assert.ok(result);
});
